#include "libmx.a"

char **parser(char *str) {

	char **islands = NULL;
	char *fts = mx_file_to_str(file)
	**islands = mx_strsplit(file, '\n');

	return islands;
}

int main(int ac, char **av) {

	for(int i = 0; av[i]; i++);
		printf("str = %s", parser(av[1]));
}